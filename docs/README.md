# TextEditor
A modular open source text editor with plugin and filetype support. It can also run and compile files.
## Plugins 
Plugins can be created by using the PluginBase module located in `./src/plugins/plugin_base`. __Please note that plugins currently have limited features as it is still in BETA.__
